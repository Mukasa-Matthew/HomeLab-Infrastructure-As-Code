---
- name: Audit Lab Environment
  hosts: all
  gather_facts: yes
  tasks:
    - name: Display OS Distribution and Python Version
      debug:
        msg: "Server {{ inventory_hostname }} is running {{ ansible_distribution }} {{ ansible_distribution_version }} with Python {{ ansible_python_version }}"
